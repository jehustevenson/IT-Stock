/**
 * @deprecated This file exists only as a compatibility shim.
 * Import types and constants directly from `@/lib/supabase/types` instead.
 * This file will be removed in a future cleanup pass.
 */
export type {
  AssetStatus,
  AssetCategory,
  AssignmentStatus,
  AuditAction,
  SchoolSection,
  Asset,
  Assignment,
  AuditLog,
} from '@/lib/supabase/types';

export { SCHOOLS } from '@/lib/supabase/types';
